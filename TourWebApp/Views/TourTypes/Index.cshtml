@model IEnumerable<TourWebApp.Models.TourType>

@{
    ViewData["Title"] = "Index";
}

<h1>Tour types list</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Label)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Label)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TourTypeID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TourTypeID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TourTypeID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
